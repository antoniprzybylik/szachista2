#pragma once

#include "global.hpp"

int cp = 8; // czarne piony
int bp = 8; // białe piony
int csp = 0; // czarne słabe piony ( zdublowane, zablokowane i samotne )
int bsp = 0; // białe słabe piony ( zdublowane, zablokowane i samotne )

int cs = 2;
int bs = 2;
int cg = 2;
int bg = 2;
int cw = 2;
int bw = 2;
int ch = 1;
int bh = 1;
int ck = 1;
int bk = 1;

int wartosc_gry = 0;

int nowa_wartosc( pair< pair<int, int>, pair<int, int> > ruch, int wg ) // przed apply
{



}

pair< pair<int, int>, pair<int, int> > generator(szachownica sz, pair<int, int> *l2m, do_roszady *drszd, typ t)
{




}
